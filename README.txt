(Header)
.Get data from url for chartjs's line (bitconchart.js lines 1-13).
.Display bitcoin chart-line (bitconchart.js lines 13-82).

(SearchBar)
.Create QR-code (dispadd.js lines 25-45) using qrcode.js library to display  (dispadd.js lines 53-54) 
.Create chartjs's doughnut (dispadd.js lines 178-211).
.Create transaction display (dispadd.js lines 122-159).
.Create oject calls storagedb for passing data through ajax POST later on (dispadd.js lines 152-165).
.Handle searching button submit (dispadd.js lines 39-54).

(Display and Updatedatabase)
.Set Up connection to MYSQL database (app.js lines 9-20).
.Using Jquery to make code shorter when i create Ajax Get request (displaybutt.js lines 1-37).
.Create app.get to handle back-end GET request (app.js lines 41-49).
.Create Ajax POST request, try to using moduel ES6 export/import data between different 
js files, in order to creating new updata.js was not successful b/c the browser was still not supported. (dispadd.js lines 214-227).
.Create app.get to handle back-end POST request (app.js lines 21-40).


